name: flutter_map_pmtiles
description: PMTiles tile provider for flutter_map
repository: https://github.com/josxha/flutter_map_plugins
issue_tracker: https://github.com/josxha/flutter_map_plugins/issues
topics: [ flutter-map, map, pmtiles ]
version: 1.0.1

environment:
  sdk: '>=3.2.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^6.1.0
  pmtiles: ^1.2.0

dev_dependencies:
  flutter_lints: ^3.0.0
